export const environment = {
  production: true,
  apiUrl: '/api',
  socketUrl: '/',
  maxFileSize: 10 * 1024 * 1024, // 10MB
  maxDuration: 10, // seconds
  maxWidth: 1024,
  maxHeight: 768,
  outputFps: 5,
  outputHeight: 400
}; 